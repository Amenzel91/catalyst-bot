# Core runtime dependencies for the catalyst bot
#
# Avoid hard pinning to specific patch versions unless strictly
# required. Allow for forward‑compatible minor updates within
# reasonable bounds to avoid dependency hell.

feedparser>=6.0,<7.0
requests>=2.31,<3.0
python-dotenv>=1.0,<2.0
vaderSentiment>=3.3,<4.0
pandas>=2.2,<3.0
numpy>=1.25,<2.0
rapidfuzz>=3.6,<4.0
discord-webhook>=1.3,<2.0
mplfinance>=0.12,<1.0
typing-extensions>=4.5,<5.0
tenacity>=9.0,<10.0